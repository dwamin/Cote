#include <iostream>
#include <string>
using namespace std;

int main()
{
    int N;
    cin >> N;
    string n;
    cin >> n;
    int sum = 0;
    for (int i = 0; i < N; i++)
    {
        sum += (n[i] - '0');
    }
    cout << sum;
}