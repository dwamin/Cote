#include <iostream>
#include <string>
#include <cmath>
using namespace std;

int main()
{
    int L;
    cin >> L;
    string s;
    long long sum = 0;
    for (int i = 0; i < L; i++)
    {
        cin >> s[i];
        sum = sum + (s[i] - 'a' + 1) * pow(31, i);
    }
    cout << sum % 1234567891;
}