#include <iostream>
using namespace std;

int main()
{
    long long int A, B, V;
    cin >> A >> B >> V;
    int day;
    day = (V - B) / (A - B);
    if ((V - B) % (A - B) != 0)
        day += 1;
    cout << day;
}